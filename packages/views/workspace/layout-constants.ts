/** Shared layout constants for the workspace UI. */

/** Tab bar height (px) — used by content panel header row. */
export const TAB_BAR_HEIGHT = 42;

/** Horizontal page inset (matches px-6 = 24px). */
export const PAGE_INSET = 24;
